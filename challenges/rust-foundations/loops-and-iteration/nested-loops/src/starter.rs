/// Generates a multiplication table as a vector of strings.
/// Each string is in the format "a x b = c".
/// TODO: Use nested for loops to generate all combinations.
pub fn multiplication_table(size: u32) -> Vec<String> {
    todo!()
}
