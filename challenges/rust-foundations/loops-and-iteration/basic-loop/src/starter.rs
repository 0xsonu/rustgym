/// Finds the first power of 2 greater than the threshold.
/// TODO: Use a loop to double a value starting from 1 until it exceeds threshold.
pub fn find_first_power_of_two_above(threshold: u32) -> u32 {
    todo!()
}
