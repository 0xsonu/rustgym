/// Analyzes text and returns (char_count, word_count, sentence_count).
/// TODO: Count non-whitespace chars, words, and sentence-ending punctuation.
pub fn analyze_text(text: &str) -> (usize, usize, usize) {
    todo!()
}
