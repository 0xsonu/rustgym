/// Returns true if value is within the range [min, max] (inclusive).
/// TODO: Use comparison operators and logical AND.
pub fn in_range(value: i32, min: i32, max: i32) -> bool {
    todo!()
}

/// Returns true if the year is a leap year.
/// TODO: A leap year is divisible by 4, but not by 100, unless also divisible by 400.
pub fn is_leap_year(year: u32) -> bool {
    todo!()
}
