/// Parses a "key=value" pair and returns the value as an integer.
/// TODO: Split at '=', parse the value, use ? with map_err.
pub fn parse_key_value(input: &str) -> Result<i32, String> {
    todo!()
}
