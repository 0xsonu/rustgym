/// Processes a list of number strings: parses, filters positives, and sums them.
/// TODO: Parse each string with ?, filter positive numbers, return their sum.
pub fn sum_positive_numbers(inputs: &[&str]) -> Result<i32, String> {
    todo!()
}
