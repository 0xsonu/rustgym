use std::num::ParseIntError;

/// Parses two strings and returns their sum.
/// TODO: Use the ? operator to parse both strings and return their sum.
pub fn add_strings(a: &str, b: &str) -> Result<i32, ParseIntError> {
    todo!()
}
