/// Parses a list of number strings and returns their sum.
/// TODO: Parse each string, accumulate the sum, return Err on failure.
pub fn sum_strings(inputs: &[&str]) -> Result<i32, String> {
    todo!()
}
