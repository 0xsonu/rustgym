/// Returns the sum and average of a borrowed vector.
/// TODO: Calculate sum and average without taking ownership of the vector.
pub fn sum_and_average(numbers: &Vec<f64>) -> (f64, f64) {
    todo!()
}
