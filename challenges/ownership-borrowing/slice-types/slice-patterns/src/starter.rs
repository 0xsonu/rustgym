/// Returns the middle portion of a slice (excluding first and last).
/// TODO: Return a sub-slice that excludes the first and last elements.
pub fn middle_elements(slice: &[i32]) -> &[i32] {
    todo!()
}
