/// Returns true if the slice contains a contiguous sub-sequence that sums to the target.
/// TODO: Check all contiguous sub-slices for a matching sum.
pub fn has_subslice_sum(numbers: &[i32], target: i32) -> bool {
    todo!()
}
