/// Splits a string at the first occurrence of a delimiter and returns both parts.
/// TODO: Find the delimiter and return slices before and after it.
pub fn split_once_at(text: &str, delimiter: char) -> (&str, &str) {
    todo!()
}
