/// Demonstrates that Copy types can be used multiple times.
/// TODO: Return (x, x*2, x*3). Notice that x can be used multiple times
/// because i32 implements Copy.
pub fn demonstrate_copy(x: i32) -> (i32, i32, i32) {
    todo!()
}
