/// Returns the length of a string created inside the function.
/// TODO: Create a String "Rust ownership" and return its length.
pub fn string_length() -> usize {
    todo!()
}

/// Creates a string from text, gets its length, then returns both.
/// TODO: Create a String from text, measure its length, return both as a tuple.
pub fn create_and_measure(text: &str) -> (String, usize) {
    todo!()
}
