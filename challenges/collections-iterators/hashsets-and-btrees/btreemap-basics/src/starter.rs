use std::collections::BTreeMap;

/// Returns scores sorted by name, keeping highest score per name.
/// TODO: Use a BTreeMap to sort by name and keep max scores.
pub fn sorted_scores(scores: &[(String, u32)]) -> Vec<(String, u32)> {
    todo!()
}
