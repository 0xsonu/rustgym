/// Returns a sorted vector with duplicates removed.
/// TODO: Sort the vector and use dedup() to remove consecutive duplicates.
pub fn unique_sorted(mut numbers: Vec<i32>) -> Vec<i32> {
    todo!()
}
