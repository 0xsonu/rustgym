use std::collections::HashMap;

/// Groups words by their length.
/// TODO: Use entry API to group words into buckets by length.
pub fn group_by_length(words: &[&str]) -> HashMap<usize, Vec<String>> {
    todo!()
}
