use std::collections::HashMap;

/// Counts the frequency of each word in the text.
/// TODO: Split by whitespace, lowercase, and count with entry API.
pub fn word_count(text: &str) -> HashMap<String, usize> {
    todo!()
}
